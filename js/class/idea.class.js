class IdeaLenguajesDelAmor {
    constructor({id, targetLanguage, idea, status }) {
        this.id = id;
        this.targetLanguage = targetLanguage;
        this.idea = idea;
        this.status = status;
    }
}